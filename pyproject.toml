[project]
name = "personal-context-protocol"
version = "0.1.3"
description = "Local-first prototype for least-privilege human context mediation between AI agents"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
